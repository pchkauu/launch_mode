name: launch_mode
description: Automatic launch modes for Flutter root and child isolates, with scoped background handler contexts and explicit initialization when needed.
version: 0.2.0
repository: https://github.com/pchkauu/launch_mode

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
